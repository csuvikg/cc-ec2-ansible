%{ for index, public_ip in public_ips ~}
ansible-test-${index} ansible_host=${public_ip}
%{ endfor ~}

[all:vars]
ansible_user=ec2-user
ansible_ssh_private_key_file=./key.pem
ansible_python_interpreter = /usr/bin/python
